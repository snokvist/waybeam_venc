# waybeam_venc feature specs (L3)

Repo-scoped, multi-session feature specs. Layer semantics and the
memory↔specs boundary are defined in the coordination repo at
`specs/README.md`. L1 invariants live in the coordination repo under
`specs/invariants/`; roadmaps under `roadmaps/`.

Each `YYYY-MM-DD-<name>/` holds `requirements.md` (mandatory) and, when
warranted, `plan.md` and `validation.md`.

| Spec | Status |
|---|---|
| `2026-07-08-maruko-stab/` | Phase 0 done (IVE unblocked); Phase 1 bench = go/no-go gate |
| `2026-07-08-ive-hw-accel/` | IVE HW proven + benchmarked; no consumer built yet |
